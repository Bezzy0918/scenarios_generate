当前场景连通组规则：
- office(0) 和 waiting_area(1) 属于同一个连通组。
- 两个 region 之间只能通过已标注的门口进入，不能从其他位置穿越区域边界。

当前场景区域连接图：
- office(0) <-> waiting_area(1)

路线在 office(0) 和 waiting_area(1) 之间移动时，必须严格使用 waypoints_by_region.json 中对应 region_transition 指定的两个 waypoint 穿越门口。
