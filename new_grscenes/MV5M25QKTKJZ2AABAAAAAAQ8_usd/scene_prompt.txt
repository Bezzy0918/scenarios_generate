当前场景连通组规则：
- seating_area(0) 属于一号组，只能在该区域内部规划路线。
- service_counter(1) 属于独立的二号组，只能在柜台内部规划路线。
- 一号组和二号组之间被柜台隔开，不能连通进入，单个行人或机器人的路线不能跨组。

当前场景区域连接图：
- 无跨区域连接。

所有路线必须始终使用同一 region 中的 waypoint，不得从 seating_area(0) 穿过柜台进入 service_counter(1)，也不得反向穿越。
